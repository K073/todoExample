export const PRODUCTION_ENV = 'production';

const config = {
  apiUrl: 'https://uxcandy.com/~shapoval/test-task-backend/v2',
  env: process.env.NODE_ENV,
  dev_name: 'Laptev_Vladislav'
};

export default config;

